import { CoverDisplayDirective } from './cover-display.directive';

describe('CoverDisplayDirective', () => {
  //it('should create an instance', () => {
  //  const directive = new CoverDisplayDirective();
  //  expect(directive).toBeTruthy();
  //});
});
